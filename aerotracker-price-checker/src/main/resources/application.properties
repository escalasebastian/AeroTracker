# RabbitMQ Connection Settings
# Spring Boot assumes localhost:5672 and guest/guest by default.
# We set them explicitly to make it easier to override later via environment variables (e.g. in Docker Compose).
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest